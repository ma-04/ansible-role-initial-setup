- hosts:
    - gcp
    #- spot
    #- infra
  roles:
    #- test
    - hostname
    - essentials
    - certbot
    - security
    - wireguard
    - docker
    #- mariadb
  #become: yes
  become_method: sudo
  gather_facts: yes
  # get vars from a file
  vars_files:
    - vars/main.yml